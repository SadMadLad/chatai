/* Types used in storage (the storage/ folder) */

export interface UserPresence {
  unique_identifier: string;
  name: string;
  avatar_url: string;
  phx_ref: string;
  online_at: string;
  username: string;
}
